
import Foundation
enum AppImages {

    enum TabIcons {
        static let home = "house"
        static let registry = "list.bullet"
        static let cart = "cart"
    }

    enum Registry {
        static let header = "registry-banner"
        static let plus = "plus"
        static let chevron = "chevron.right"
    }
}
